<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- JSON Logging with Logstash encoder -->
    <appender name="JSON" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="net.logstash.logback.encoder.LogstashEncoder">
            <!-- Static fields -->
            <customFields>{"service":"${spring.application.name:-customer-service}"}</customFields>
            
            <!-- Include MDC fields (correlation ID, user ID, etc.) -->
            <includeMdcKeyName>correlationId</includeMdcKeyName>
            <includeMdcKeyName>userId</includeMdcKeyName>
            <includeMdcKeyName>customerId</includeMdcKeyName>
            <includeMdcKeyName>traceId</includeMdcKeyName>
            <includeMdcKeyName>spanId</includeMdcKeyName>
            
            <!-- Standard fields -->
            <timestampPattern>yyyy-MM-dd'T'HH:mm:ss.SSSZ</timestampPattern>
            <fieldNames>
                <timestamp>timestamp</timestamp>
                <message>message</message>
                <logger>logger</logger>
                <level>level</level>
                <thread>thread</thread>
                <version>[ignore]</version>
            </fieldNames>
        </encoder>
    </appender>

    <!-- Root logger using JSON appender -->
    <root level="INFO">
        <appender-ref ref="JSON" />
    </root>
    
    <!-- Special config for development -->
    <springProfile name="dev">
        <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
            <encoder>
                <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg [traceId=%X{traceId}, spanId=%X{spanId}, correlationId=%X{correlationId}]%n</pattern>
            </encoder>
        </appender>
        <root level="INFO">
            <appender-ref ref="CONSOLE" />
        </root>
    </springProfile>
</configuration>